import styled from 'styled-components'
import {categories} from '../data.js'
import categorieItem

const Container = styled.div`

`;

const Categories = () => {
    return (
        <Container>
            {
                categories.map(item=>(

                ))
            }
        </Container>
    );
};

export default Categories;